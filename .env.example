# BPLab Trace LIMS V11 — 环境配置
# 复制此文件为 .env 并填入实际值
# cp .env.example .env

# ── 数据库 (PostgreSQL) ──
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=bplab123
DB_NAME=bplab

# ── 安全 ──
# 生成密钥: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=change-me-to-a-random-secret-key
JWT_EXPIRE_MINUTES=10080
JWT_REFRESH_DAYS=30

# ── CORS (前端地址，逗号分隔) ──
# 开发: http://localhost:5173
# 生产: http://localhost
CORS_ORIGINS=["http://localhost:5173","http://localhost"]

# ── 调试模式 ──
DEBUG=false

# ── 上传目录 ──
UPLOAD_DIR=data/uploads
ATTACHMENT_DIR=data/attachments
SIGNATURE_DIR=data/signatures

# ── 种子数据 CSV (可选) ──
# 首次启动时自动从 CSV 导入设备和绑定
# 将文件放入 资料/bplab_v10_update/bplab_v10_template_update/ 目录
SEED_EQUIPMENT_CSV=
SEED_BINDING_CSV=
